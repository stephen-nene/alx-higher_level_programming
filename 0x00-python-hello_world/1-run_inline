#!/bin/bash

# Check if the PYCODE environment variable is set
if [ -z "$PYCODE" ]; then
  echo "Error: PYCODE environment variable is not set."
  exit 1
fi

# Execute the Python code stored in the PYCODE variable
python3 -c "$PYCODE"
